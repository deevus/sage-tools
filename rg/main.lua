local rg = require("rg")
local find_files = require("find_files")
local find_references = require("find_references")

sage.register_tool(rg)
sage.register_tool(find_files)
sage.register_tool(find_references)
